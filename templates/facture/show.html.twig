{% extends 'base2.html.twig' %}

{% block title %}Facture{% endblock %}

{% block body %}
    <div class='title'>
        Factures	<a href="#" style="float: right;" class="light"></a>
    </div>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ facture.id }}</td>
            </tr>
            <tr>
                <th>DateFacture</th>
                <td>{{ facture.DateFacture ? facture.DateFacture|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Total</th>
                <td>{{ facture.Total }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('facture_index') }}"><img src="{{ asset('assets/images/back.png') }}" alt="Symfony!" height="42" width="42" /></a>

    <a href="{{ path('facture_edit', {'id': facture.id}) }}"><img src="{{ asset('assets/images/edit.png') }}" alt="Symfony!" height="42" width="42" /></a>

    {{ include('facture/_delete_form.html.twig') }}
{% endblock %}

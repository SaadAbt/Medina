{% extends 'base2.html.twig' %}

{% block title %}Commentaire{% endblock %}

{% block body %}
    <div class='title'>
        Commentaire	<a href="#" style="float: right;" class="light"></a>
    </div>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commentaire.id }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ commentaire.text }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ commentaire.Date ? commentaire.Date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('commentaire_index') }}"><img src="{{ asset('assets/images/back.png') }}" alt="Symfony!" height="42" width="42" /></a>

    <a href="{{ path('commentaire_edit', {'id': commentaire.id}) }}"><img src="{{ asset('assets/images/edit.png') }}" alt="Symfony!" height="42" width="42" /></a>

    {{ include('commentaire/_delete_form.html.twig') }}
{% endblock %}

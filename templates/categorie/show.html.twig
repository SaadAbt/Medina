{% extends 'base2.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <div class='title'>
        Catégories	<a href="#" style="float: right;" class="light"></a>
    </div>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>NomCategorie</th>
                <td>{{ categorie.NomCategorie }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('categorie_index') }}"><img src="{{ asset('assets/images/back.png') }}" alt="Symfony!" height="42" width="42" /></a>

    <a href="{{ path('categorie_edit', {'id': categorie.id}) }}"><img src="{{ asset('assets/images/edit.png') }}" alt="Symfony!" height="42" width="42" /></a>

    {{ include('categorie/_delete_form.html.twig') }}
{% endblock %}
